{
  "name": "pseudo_random_number_example",
  "version": "0.0.0",
  "description": "An example of how the p5 random function distrobutes random numbers.",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html"
  },
  "keywords": ["processing", "typescript"],
  "author": "william phillpotts",
  "dependencies": {
    "@types/p5": "^1.4.3",
    "p5": "^1.5.0"
  },
  "devDependencies": {
    "parcel": "^2.8.2"
  }
}
